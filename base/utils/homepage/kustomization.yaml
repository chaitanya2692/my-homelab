---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: utils

resources:
  - deployment.yaml
  - service.yaml
  - rbac.yaml
  - serviceaccount.yaml
  - configmap.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: homepage
