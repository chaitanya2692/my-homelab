---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-issuer
spec:
  acme:
    server: placeholder-server
    email: admin@my-homelab.party
    privateKeySecretRef:
      name: placeholder-name
    solvers:
      - http01:
          ingress:
            class: traefik-external
